github:

git status
git remote add origin git@github.com:yingyingbi/gittest.git

git add filename
git add .
git commit -m "XXX"
git push -u origin master


# removes the file from the repo but also deletes it from the local file system.
git rm file1.txt  ## remove the file from the dick and stage the removal
git commit -m "remove file1.txt" 
To remove the file from the repo and not delete it from the local file system use:
git rm --cached file1.txt

git diff
git diff HEAD: diff of our most recent commit which is referred by the HEAD pointer
git diff –staged

git fetch
git rebase
## if conflict, fix it
git add .
git rebase –continue
git push origin master

## if no conflict
git add. 
git commit –m “”
git push origin master

git rebase –abort

git pull origin master

git branch
git branch feature1
git checkout feature1
git checkout master
git checkout –b feature1

git merge branchname
git branch –d branchname: delete a branch

In the simplest terms, git pull does a git fetch followed by a git merge.

git add directory/file

git reset file: upstaging

## move around
git checkout -- <target> : change back to how they were at the last commit 
git checkout master^
git checkout HEAD^ : move to the parent commit(one level up)- caret ^
git checkout HEAD~4: move to 4 level up: tilde ~
git branch –f  master HEAD~3: reassign a branch(master) to a commit three parents behind HEAD

